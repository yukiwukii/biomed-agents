default_language_version:
  python: python3
repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: check-added-large-files
        # Default is 500 KB. This repo deliberately tracks run artifacts as a
        # record of the experiments: the proposer's generated-hypothesis dumps
        # (~1.9 MB each), its rendered rubric viewer, the analysis notebooks with
        # their outputs, and the combined RL data split. 2 MB covers those with
        # headroom while still catching an accidental capsule or model checkpoint.
        args: [--maxkb=2048]
        exclude: uv.lock
      - id: check-case-conflict
      - id: check-merge-conflict
      - id: check-shebang-scripts-are-executable
      - id: check-symlinks
      - id: check-toml
      - id: check-yaml
      - id: detect-private-key
      - id: end-of-file-fixer
        exclude: ^tests/assets/
      - id: fix-byte-order-marker
      - id: mixed-line-ending
        exclude: .gitignore
      - id: trailing-whitespace
        exclude: .gitignore|^tests/assets/
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.14.2
    hooks:
      - id: ruff-format
        exclude: ^tests/assets/
      - id: ruff-check
        args: [--fix, --exit-non-zero-on-fix]
        exclude: ^tests/assets/
  - repo: https://github.com/rbubley/mirrors-prettier
    rev: v3.6.2
    hooks:
      - id: prettier
        exclude: ^tests/assets/
  - repo: https://github.com/google/yamlfmt
    rev: v0.20.0
    hooks:
      - id: yamlfmt
  - repo: https://github.com/Yelp/detect-secrets
    rev: v1.5.0
    hooks:
      - id: detect-secrets
        additional_dependencies: [".[word_list]"]
        args:
          - --exclude-files=.secrets.baseline$
          - --exclude-files=.*\.ipynb$
  - repo: https://github.com/pappasam/toml-sort
    rev: v0.24.3
    hooks:
      - id: toml-sort-fix
  - repo: https://github.com/codespell-project/codespell
    rev: v2.4.1
    hooks:
      - id: codespell
        additional_dependencies: [".[toml]"]
        exclude_types: [jupyter]
  - repo: https://github.com/astral-sh/uv-pre-commit
    rev: 0.9.5
    hooks:
      - id: uv-lock
  - repo: local
    hooks:
      - id: mypy
        name: mypy
        entry: uv run mypy
        language: system
        types: [python]
        args: ["--scripts-are-modules"]
  - repo: https://github.com/henryiii/validate-pyproject-schema-store
    rev: 2025.10.25
    hooks:
      - id: validate-pyproject
