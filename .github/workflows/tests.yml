name: Lint and Test
on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:
jobs:
  pre-commit:
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    strategy:
      matrix:
        python-version: ["3.11", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
          python-version: ${{ matrix.python-version }}
          activate-environment: true
      - run: uv sync
      - run: uv run prek run -a
      - uses: pre-commit-ci/lite-action@v1.1.0
        if: always()
  test:
    runs-on: ubuntu-latest
    env:
      # GitHub does not pass secrets to pull_request runs from forks, so this is
      # empty for outside contributors. That is fine: the tests needing a key are
      # marked `requires_openai` (tests/conftest.py) and skip when it is unset,
      # so a fork PR gets a green run instead of a spurious failure.
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
    strategy:
      matrix:
        python-version: ["3.11", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
          python-version: ${{ matrix.python-version }}
      - run: uv sync
      - run: uv run pytest -n auto
  ci:
    runs-on: ubuntu-latest
    needs: [pre-commit, test]
    if: always()
    steps:
      - run: |
          if [[ "${{ needs.pre-commit.result }}" != "success" && "${{ needs.pre-commit.result }}" != "skipped" ]]; then
            exit 1
          fi
          if [[ "${{ needs.test.result }}" != "success" ]]; then
            exit 1
          fi
